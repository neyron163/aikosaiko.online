input create {
  name: String
  img: String
  age: Int
}

type productsType {
  id: ID!
  name: String
  img: String
  age: Int
}

type Query {
  products: [productsType!]!
}

type Mutation {
  createProduct(input: create): productsType
}